apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    name: vredis-pod
    app: voting-app
spec:
  containers:
  - name: redis
    image: redis
    ports:
      - containerPort: 6379
    resources:
      limits:
        cpu: "1"
        memory: "512Mi"
